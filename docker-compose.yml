services:
  app:
    build: .
    ports:
      - '3000:3000'
    environment:
      NODE_ENV: production
      # DATABASE_URL: postgres://prep:prep@db:5432/prep_tracker
    env_file:
      - path: .env
        required: false
    restart: unless-stopped
    # depends_on:
    #   db:
    #     condition: service_healthy

  # Uncomment once the app gets a real database.
  # db:
  #   image: postgres:16-alpine
  #   environment:
  #     POSTGRES_USER: prep
  #     POSTGRES_PASSWORD: prep
  #     POSTGRES_DB: prep_tracker
  #   volumes:
  #     - db_data:/var/lib/postgresql/data
  #   ports:
  #     - '5432:5432'
  #   healthcheck:
  #     test: ['CMD-SHELL', 'pg_isready -U prep -d prep_tracker']
  #     interval: 5s
  #     timeout: 5s
  #     retries: 5
  #   restart: unless-stopped

# volumes:
#   db_data:
